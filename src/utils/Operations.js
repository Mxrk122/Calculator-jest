const square = (number) => parseInt(number, 10) * parseInt(number, 10)

export { square }

const cube = (number) => parseInt(number, 10) * parseInt(number, 10) * parseInt(number, 10)

export { cube }
